spring:
  application:
    name: easyEvent
  datasource:

    url: jdbc:mariadb://localhost:3308/easy_event
    username: root
    password: 1604
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
